apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netbox-dev
  namespace: openshift-operators # or argocd namespace
  labels:
    app: netbox
    environment: dev
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: netbox

  source:
    repoURL: https://gitlab.presidio.com/rgomez/netbox-development.git  # Update with your repo URL
    targetRevision: main  # or dev branch
    path: k8s/overlays/dev

  destination:
    server: https://api.rhocp-cluster.presidiolab.local:6443
    namespace: netbox-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: route.openshift.io
      kind: Route
      jsonPointers:
        - /status
